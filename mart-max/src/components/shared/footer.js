import React, { Component } from 'react';

export default class Footer extends Component {
    render = () => (
        <footer className="App-footer">
            <nav>Footer stays here</nav>
        </footer>
    )
}